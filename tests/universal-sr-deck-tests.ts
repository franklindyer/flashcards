describe('SR deck runs without errors', () => {
    // test('new and due cards can be studied', () => {});
    // test('menu can be opened and modified without errors', () => {});
    // test('all sorts of malformed SR decks are repaired correctly', () => {});
});

describe('SR deck logic is correct', () => {
    // test('new cards transition to due after enough correct answers', () => {});
    // test('a due card's interval gets longer iff answered correctly', () => {});
    // test('practice cards do not affect the deck's state', () => {});
    // test('cards become due at the time they are scheduled to', () => {});
});
